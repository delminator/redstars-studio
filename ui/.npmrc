# @delminator/core-ui (types-only contract) is on GitHub Packages (private).
# CI feeds NODE_AUTH_TOKEN from the GH_PACKAGES_TOKEN secret (read:packages PAT).
@delminator:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
